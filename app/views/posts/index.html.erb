<h1>Football rumours</h1>
<%= form_for(@post) do |f| %>
  <p>
  <%= f.label :body, "Post:"  %><br>
  <%= f.text_area :body %>
  </p>
  <div>
  <%= f.submit  %>
  </div>
<% end %>
<% @posts.each do |post| %>
  <div>
    <b><%= post.user.username %></b>
    <%= post.body %>
  </div>
  <% end %>